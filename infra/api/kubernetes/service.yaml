apiVersion: v1
kind: Service
metadata:
  name: jupiter-api
  namespace: jupiter
spec:
  selector:
    app: jupiter-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
