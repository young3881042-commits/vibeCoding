services:
  web:
    build:
      context: ../../../apps/web
      dockerfile: ../../infra/web/docker/Dockerfile
    image: jupiter-web:local
    env_file:
      - web.env
    ports:
      - "${WEB_PORT:-5173}:5173"
    extra_hosts:
      - "host.docker.internal:host-gateway"
